stages:
  - test
  - build
  - deploy

apollo-test:
  stage: test
  tags:
    - apollo
  script:
    - echo "Test is good!"

apollo-build:
  stage: build
  tags:
    - apollo
  script:
    - echo "Apollo Is Builded"

# apollo-deploy-uat:
#   stage: deploy
#   tags:
#     - apollo
#     - uat
#   script:
#     - echo "Deploy on UAT is finished!"
#   only:
#     - master

apollo-deploy-prod:
  stage: deploy
  # when: manual
  tags:
    - apollo
    - prod
  script:
    - echo "Deploy on $PG_USERNAME is finished!"
  only:
    - master
