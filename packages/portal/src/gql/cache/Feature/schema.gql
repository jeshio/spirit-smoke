interface IFeature {
  isSelectedForProductCategory(productCategorySlug: ID!): Boolean
}

type FeatureSimple {
  isSelectedForProductCategory(productCategorySlug: ID!): Boolean
}

type Feature {
  isSelectedForProductCategory(productCategorySlug: ID!): Boolean
}