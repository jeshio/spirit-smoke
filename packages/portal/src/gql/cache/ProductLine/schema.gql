interface IProductLine {
  isSelectedForProductCategory(productCategorySlug: ID!): Boolean
}

type ProductLineSimple {
  isSelectedForProductCategory(productCategorySlug: ID!): Boolean
}

type ProductLine {
  isSelectedForProductCategory(productCategorySlug: ID!): Boolean
}